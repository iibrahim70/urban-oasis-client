import heroImage from "@/assets/images/hero.png";

const Hero = () => {
  return (
    <section>
      <img src={heroImage} alt="Hero Image" className="w-full" />
    </section>
  );
};

export default Hero;
