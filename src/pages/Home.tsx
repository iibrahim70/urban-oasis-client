import Hero from "@/components/Hero";
import PropertyListings from "@/components/PropertyListings";

const Home = () => {
  return (
    <main>
      <Hero />
      <PropertyListings />
    </main>
  );
};

export default Home;
